process HcalValid  = {

   service = MessageLogger	{
      untracked vstring destinations = {"cout"} 
      untracked vstring categories = { "HcalSim" }

      untracked vstring debugModules = { "*" }
      untracked PSet cout = { untracked string threshold = "INFO"
         untracked PSet default = { untracked int32 limit = 0 }
         untracked PSet HcalSim = { untracked int32 limit = -1 }
      }
   }


   untracked PSet maxEvents = {untracked int32 input = -1}

   source = PoolSource {
      untracked vstring fileNames = {'file:simevent.root'}
      untracked uint32 debugVebosity = 11
      untracked bool   debugFlag     = true
   }

   service = DaqMonitorROOTBackEnd{ }

   #Magnetic Field 		
   include "MagneticField/Engine/data/volumeBasedMagneticField.cfi"

   #Geometry
   include "SimG4CMS/Calo/test/data/testGeometryXML.cfi"

   include "SimG4CMS/Calo/test/data/HcalSimHitStudy.cfi"
   replace hcalSimHitStudy.OutputFile    = "hcalsimstudy.root"

   path p1 = { hcalSimHitStudy }

}		
